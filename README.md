# AI-Deployment-Agent
This is a a smart deployment agent that takes basic inputs like image ID, environment name, and repository name. And based on this, it will automatically trigger the correct GitHub Action workflow(pipleine). The agent will check if the inputs are valid and choose the right deployment process.
